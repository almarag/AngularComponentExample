import controller from './servicesExample.controller';
import template from './servicesExample.html';

const servicesExampleComponent = {
  bindings: {},
  controller,
  restrict: 'E',
  template,
};

export default servicesExampleComponent;
