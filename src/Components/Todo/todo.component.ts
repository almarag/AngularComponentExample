import controller from './todo.controller';
import template from './todo.html';

const todoComponent = {
    bindings: {},
    controller,
    restrict: 'E',
    template
};

export default todoComponent;
